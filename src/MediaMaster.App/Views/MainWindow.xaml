<Window x:Class="Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:tb="clr-namespace:H.NotifyIcon;assembly=H.NotifyIcon.Wpf"
        xmlns:local="clr-namespace:MediaMaster.App.Views"
        mc:Ignorable="d"
        Title="MediaMaster" Height="506" Width="568" MinHeight="400" MinWidth="480"
        Background="{DynamicResource Theme.WindowBackground}" Foreground="{DynamicResource Theme.Foreground}">

    <Window.Resources>
        <ContextMenu x:Key="TrayContextMenu">
            <MenuItem Header="Restore" Click="OnTrayRestoreClick" />
            <Separator />
            <MenuItem Header="Quit" Click="OnTrayQuitClick" />
        </ContextMenu>
    </Window.Resources>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Settings">
                <MenuItem Header="General" Click="OnOpenSettingsClick" />
                <MenuItem Header="Traffic Info" IsEnabled="False" />
                <MenuItem Header="Source" Click="OnOpenSourceSettingsClick" />
                <MenuItem Header="Destination" Click="OnOpenDestinationSettingsClick" />
                <Separator />
                <MenuItem Header="Quit" Command="{Binding QuitCommand}" />
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="Minimize to tray" Command="{Binding MinimizeToTrayCommand}" />
                <Separator />
                <MenuItem Header="Theme">
                    <MenuItem x:Name="ThemeLightMenuItem" Header="Light" Click="OnThemeLightClick" />
                    <MenuItem x:Name="ThemeDarkMenuItem" Header="Dark" Click="OnThemeDarkClick" />
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Info">
                <MenuItem Header="Registration" IsEnabled="False" />
                <MenuItem Header="About" IsEnabled="False" />
            </MenuItem>
        </Menu>

        <tb:TaskbarIcon x:Name="TrayIcon"
                         DockPanel.Dock="Top"
                         Height="0"
                         ToolTipText="MediaMaster"
                         ContextMenu="{StaticResource TrayContextMenu}"
                         TrayLeftMouseUp="OnTrayRestoreClick">
            <tb:TaskbarIcon.IconSource>
                <tb:GeneratedIconSource Text="M" Background="#2D6CDF" Foreground="White" FontWeight="Bold" />
            </tb:TaskbarIcon.IconSource>
        </tb:TaskbarIcon>

        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding StatusText}" FontSize="14" Margin="0,0,0,8" />

            <ProgressBar Grid.Row="1" Height="16" Margin="0,0,0,12"
                         Minimum="0"
                         Maximum="{Binding ProgressMaximum}"
                         Value="{Binding ProgressValue}" />

            <TextBlock Grid.Row="2" Text="Log" FontWeight="Bold" Margin="0,0,0,4" />

            <ListBox Grid.Row="3" x:Name="LogListBox" ItemsSource="{Binding LogEntries}" FontFamily="Consolas">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Display}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </DockPanel>
</Window>
